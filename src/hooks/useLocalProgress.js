// PATH: src/hooks/useLocalProgress.js

/**
 * Progress hook — placeholder for future implementation.
 * The old progress tracking relied on the 7-year module completion model.
 * This stub preserves the API surface for components that may use it.
 */
const useLocalProgress = () => {
    return {
        getYearProgress: () => 0,
        markModuleComplete: () => { },
        isModuleCompleted: () => false,
    };
};

export default useLocalProgress;
